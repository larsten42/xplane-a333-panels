# Dataref & command inventory

Every X-Plane dataref and command this app actually references, pulled
straight from `config/profiles/*.json` — those files are the real source of
truth (see `ARCHITECTURE.md`'s "Adding support for another aircraft" for
their shapes); this is a flattened, human-scannable index over them,
generated by walking each profile's `keys`/`buttons`/`toggleSwitches`/
`readouts` and collecting every dataref/command string. If a profile
changes, re-derive this rather than hand-editing it out of sync.

Datarefs/commands that were found live but never wired into the UI (see
`ARCHITECTURE.md`'s "Known limitations" — e.g. MCDU brightness/annunciator
datarefs on some aircraft, the transponder mode enum) aren't listed here;
this is what's actually in use, not everything ever discovered.

---

## MCDU

Identical dataref/command set for both `default-fms.json` (A330) and
`b738-fms.json` (737-800) — same underlying X-Plane FMS/CDU system, only
`keypadLayout`/key `label` overrides differ between the two. See
`ARCHITECTURE.md` for why that's a low-effort shared mapping rather than
two independent ones.

### Datarefs

`{cdu}` = 1/2/3, `{line}` = 0–13 (14 lines/CDU × 3 CDUs × 2 = 84 concrete datarefs):

- `sim/cockpit2/radios/indicators/fms_cdu{cdu}_text_line{line}`
- `sim/cockpit2/radios/indicators/fms_cdu{cdu}_style_line{line}`

### Commands

Every key command below is composed as `{prefix}/{suffix}`, where `{prefix}`
is `sim/FMS` (CDU1), `sim/FMS2` (CDU2), or `sim/CDU3` (CDU3) — i.e. every
suffix listed exists three times, once per CDU.

| Group | Suffixes |
|---|---|
| Numeric | `key_0` … `key_9` |
| Alpha | `key_A` … `key_Z` |
| Punctuation | `key_period`, `key_minus`, `key_slash`, `key_space`, `key_clear`, `key_delete`, `key_back`, `key_overfly` |
| Line select keys | `ls_1l`/`ls_1r` … `ls_6l`/`ls_6r` |
| Function keys | `index`, `fpln`, `clb`, `crz`, `des`, `dir_intc`, `legs`, `dep_arr`, `hold`, `exec`, `fix`, `navrad`, `prev`, `next`, `airport`, `atc_comm`, `data`, `fuel_pred`, `menu`, `perf`, `prog`, `sec_fpln`, `up`, `down`, `CDU_popup`, `CDU_popout` |

**A330 only** — brightness, not `commandPrefixByCdu`-based (Airbus-specific,
`{cdu}` substituted directly): `laminar/A333/buttons/fms{cdu}_brightness_up`,
`laminar/A333/buttons/fms{cdu}_brightness_dn`. No 737-800 equivalent was
found live — see `ARCHITECTURE.md`'s "Known limitations".

---

## EFIS (`efis-a333.json`, Airbus A330 only)

### Datarefs

- `sim/cockpit/electrical/avionics_on`
- `sim/cockpit/misc/barometer_setting`
- `sim/cockpit/switches/EFIS_map_range_selector`
- `sim/cockpit2/EFIS/EFIS_1_selection_pilot`
- `sim/cockpit2/EFIS/EFIS_2_selection_pilot`
- `sim/cockpit2/annunciators/flight_director`
- `sim/cockpit2/gauges/actuators/barometer_setting_in_hg_pilot`
- `sim/cockpit2/gauges/actuators/barometer_setting_is_std_pilot`
- `laminar/A333/annun/EFIS_capt_arpt`
- `laminar/A333/annun/EFIS_capt_cstr`
- `laminar/A333/annun/EFIS_capt_fix`
- `laminar/A333/annun/EFIS_capt_ndb`
- `laminar/A333/annun/EFIS_capt_vor`
- `laminar/A333/annun/captain_ls_bars_on`
- `laminar/A333/barometer/capt_inHg_hPa_pos`
- `laminar/A333/knobs/EFIS_mode_pos_capt`

### Commands

- `sim/autopilot/fdir_command_bars_toggle`
- `sim/instruments/EFIS_apt`, `EFIS_fix`, `EFIS_ndb`, `EFIS_vor`
- `sim/instruments/EFIS_1_pilot_sel_adf`, `_off`, `_vor`
- `sim/instruments/EFIS_2_pilot_sel_adf`, `_off`, `_vor`
- `sim/instruments/barometer_up`, `barometer_down`
- `laminar/A333/buttons/capt_EFIS_CSTR`
- `laminar/A333/buttons/capt_ils_bars_push`
- `laminar/A333/knob/baro/capt_inHg`, `capt_hPa`
- `laminar/A333/knobs/capt_EFIS_knob_left`, `capt_EFIS_knob_right`
- `laminar/A333/pull/baro/capt_std`
- `laminar/A333/push/baro/capt_std`

---

## FCU (`fcu-a333.json`, Airbus A330 only)

### Datarefs

- `sim/cockpit/autopilot/airspeed`, `airspeed_is_mach`, `altitude`, `heading_mag`, `vertical_velocity`
- `sim/cockpit/electrical/avionics_on`
- `sim/cockpit2/autopilot/altitude_hold_armed`, `approach_status`, `autothrottle_arm`, `fms_vnav`, `trk_fpa`, `vnav_speed_window_open`
- `laminar/A333/PFD/FMAs/autopilot_12_status`, `nav_loc_arm_status`
- `laminar/A333/autopilot/alt_step_knob_pos`, `hdg_window_open`, `vvi_fpa_window_open`

### Commands

- `sim/autopilot/servos_toggle`, `servos2_toggle` (AP1/AP2)
- `sim/autopilot/autothrottle_arm`, `autothrottle_hard_off`
- `sim/autopilot/trkfpa`, `altitude_hold`, `approach`, `NAV`, `knots_mach_toggle`
- `sim/autopilot/alt_intv`, `FMS`, `level_change`, `speed_hold`
- `sim/autopilot/heading_sync`, `gpss`
- `laminar/A333/autopilot/a_thr_toggle`
- `laminar/A333/autopilot/alt_step_left`, `alt_step_right`
- `laminar/A333/autopilot/altitude_knob_pull`, `altitude_knob_push`
- `laminar/A333/autopilot/heading_knob_pull`, `heading_knob_push`
- `laminar/A333/autopilot/speed_knob_pull`, `speed_knob_push`
- `laminar/A333/autopilot/vertical_knob_pull`, `vertical_knob_push`
- `laminar/A333/autopilot/metric_alt_push`

---

## Radio (`radio-panel-generic.json`, aircraft-generic)

Lives under X-Plane's own generic `sim/radios`/`sim/audio_panel`/
`sim/transponder`/`sim/cockpit2/radios` namespaces rather than an
aircraft-specific one — confirmed live against the stock King Air C90 and
the A330/737-800, see the profile's own `description`.

### Datarefs

- `sim/cockpit2/radios/actuators/com{1,2}_frequency_hz_833`, `com{1,2}_standby_frequency_hz_833`
- `sim/cockpit2/radios/actuators/nav{1,2}_frequency_hz`, `nav{1,2}_standby_frequency_hz`
- `sim/cockpit2/radios/actuators/dme_frequency_hz`, `dme_standby_frequency_hz` (a single physical DME shared by the whole panel — no `dme1`/`dme2`-numbered frequency dataref exists at all, confirmed live)
- `sim/cockpit2/radios/actuators/adf{1,2}_frequency_hz`, `adf{1,2}_standby_frequency_hz`
- `sim/cockpit2/radios/actuators/transponder_code`
- `sim/cockpit2/radios/actuators/audio_selection_com1`, `com2`, `nav1`, `nav2`, `adf1`, `adf2`, `dme1`
- `sim/cockpit2/radios/actuators/audio_com_selection` (MIC SEL state — not a clean 0/1 enum, read by parity, see the profile's own `_note`)

### Commands

- `sim/radios/com1_standy_flip`, `com2_standy_flip`, `nav1_standy_flip`, `nav2_standy_flip`, `adf1_standy_flip`, `adf2_standy_flip` (ACT/STBY swap — "standy" is X-Plane's own real spelling, not a typo here)
- `sim/radios/dme_standby_flip` (correctly spelled, unlike the others)
- `sim/audio_panel/monitor_audio_com1`, `com2`, `nav1`, `nav2`, `adf1`, `adf2`, `dme`
- `sim/audio_panel/transmit_audio_com1`, `transmit_audio_com2` (MIC SEL — one-shot select, not toggles; deliberately not the `_man` variants, which X-Plane's own description flags as "old panel, doesn't change listener")
- `sim/transponder/transponder_12_up`, `_down`, `transponder_34_up`, `_down` (resolved but not yet wired to the UI — see "Known limitations")

Standby tuning (COM/NAV/DME/ADF1/ADF2) writes the standby-frequency dataref
directly rather than firing step commands — see `src/radio-panel.js`'s own
top comment for why. The `stby_*_coarse/fine_up/down` step commands aren't
listed above since nothing in the app fires them anymore.

---

## RMP+ACP (`rmp-acp-a333.json`, Airbus A330 only)

Stock/default A330's own RTP (X-Plane's name for the real-hardware RMP) and
ACP, Captain's side only, VHF1/VHF2 scoped for now — see the profile's own
`description`/`_note_on_*` fields for the fuller reasoning. Under
`laminar/A333/rtp_L/...` and `laminar/A333/audio/capt/...`, a completely
separate namespace from ToLiss's `AirbusFBW/RMP{1,2,3}` (different aircraft,
different implementation of the same real-world concept).

### Datarefs

- `laminar/A333/comm/rtp_L/vhf_1_status`, `vhf_2_status` (two independent booleans, not a shared enum, confirmed live)
- `laminar/A333/comm/rtp_L/off_status` (1 = off, 0 = on, confirmed live)
- `laminar/A333/audio/capt/mic_status1`, `mic_status2` (confirmed live mutually exclusive)
- `sim/cockpit2/radios/actuators/com{1,2}_frequency_hz_833`, `com{1,2}_standby_frequency_hz_833` (same datarefs the generic radio panel already uses — see `_note_on_tuning`)
- `laminar/A333/audio/capt/volume_pos_0`, `volume_pos_1` (read fine; writing a fractional value fails live with an X-Plane-side "incompatible_data" error — see the profile's own `_gap_acp_volume_write`)
- `laminar/A333/audio/capt/listen_status` (16-element array, confirmed live index 0 = VHF1/index 1 = VHF2, plain 0/1 — resolved/subscribed directly in `src/rmp-panel.js`, not through the profile, since EfisAdapter's button model doesn't support one shared array dataref with a per-button index)

### Commands

- `laminar/A333/rtp_L/vhf_1/sel_switch`, `vhf_2/sel_switch` (channel select)
- `laminar/A333/rtp_L/freq_txfr/sel_switch` (active/standby transfer)
- `laminar/A333/rtp_L/off_switch` (power toggle, confirmed live)
- `laminar/A333/audio/capt/mic_push1`, `mic_push2` (transmit select)
- `laminar/A333/audio/capt/listen_press00`, `listen_press01` (listen toggle — index 0/1 one-to-one, confirmed live)

The RTP's own `freq_khz`/`freq_MHz` `sel_dial_up`/`_dn` step commands are
resolved but not used — tuning reuses the generic radio panel's direct
standby-dataref write instead, see `_note_on_tuning`. Not yet wired at all:
VHF3/HF1/HF2/AM/NAV/VOR/LS/ADF/BFO on the RTP, and the ACP's INT/CAB/PA/
nav-reception rows.
